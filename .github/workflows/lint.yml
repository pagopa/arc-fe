name: 'lint'
on:
  pull_request:
    branches:
      - develop

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run lint
        run: |
          yarn install
          yarn lint
